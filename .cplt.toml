# Generated by `cplt init` — review before trusting.
# Detected: JVM (Gradle), Docker, Ktor, Flyway
# Workspace members:
#   dato (settings.gradle) — JVM (Gradle)
#   modell (settings.gradle) — JVM (Gradle)
#   openapi (settings.gradle) — JVM (Gradle)
#   opplysninger (settings.gradle) — JVM (Gradle)
#   dagpenger (settings.gradle) — JVM (Gradle)
#   ferietillegg (settings.gradle) — JVM (Gradle)
#   cucumber-testsupport (settings.gradle) — JVM (Gradle)
#   utestengning (settings.gradle) — JVM (Gradle)
#   regelverk (settings.gradle) — JVM (Gradle)
#   mediator (settings.gradle) — JVM (Gradle), Docker, Ktor, Flyway
#   avklaring (settings.gradle) — JVM (Gradle)
#   dag (settings.gradle) — JVM (Gradle)
#   konfigurasjon (settings.gradle) — JVM (Gradle)
#   uuid-v7 (settings.gradle) — JVM (Gradle)

[propose]
# ⚠️  grants access to Docker socket — effectively root on the host
# Suggested by: mediator
allow_docker = true
# Suggested by: (root), avklaring, cucumber-testsupport, dag, dagpenger, dato, ferietillegg, konfigurasjon, mediator, modell, openapi, opplysninger, regelverk, utestengning, uuid-v7
allow_jvm_attach = true

[propose.allow]
ports = [5432]
localhost = [8080]

# The following are machine-specific — add to ~/.config/cplt/config.toml:
# allow.read += ["~/.gradle/gradle.properties"]  # in personal config

